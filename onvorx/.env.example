# Supabase — public (anon) read access, used only by the build-time content step.
# Set these in Vercel → Project → Settings → Environment Variables (all environments).
# Leave unset locally to build with the committed base content.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR-ANON-KEY
